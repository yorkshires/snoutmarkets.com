import { Resend } from 'resend'; const resendKey=process.env.RESEND_API_KEY||''; const fromEmail=process.env.FROM_EMAIL||'SnoutMarkets <noreply@example.com>'; export async function sendEmail(to:string,subject:string,html:string){ if(!resendKey){ console.warn('RESEND_API_KEY not set. Skipping email send:', subject); return {id:'dev-skip'}; } const resend=new Resend(resendKey); return await resend.emails.send({from:fromEmail,to,subject,html}); }