export function currencyDKK(amountCents:number,currency='DKK'){ return (amountCents/100).toLocaleString('da-DK',{style:'currency',currency}); } export function cn(...classes:(string|false|undefined|null)[]){ return classes.filter(Boolean).join(' '); }