import Link from 'next/link'; import { currencyDKK } from '@/lib/utils'; import Image from 'next/image'; export default function ListingCard({ listing }: { listing:any }){ const price=currencyDKK(listing.priceCents, listing.currency||'DKK'); return (<Link href={`/listing/${listing.id}`} className='card block hover:shadow-lg transition-shadow'>{listing.imageUrl?(<Image src={listing.imageUrl} alt={listing.title} width={900} height={600} className='w-full h-48 object-cover rounded-xl mb-3'/>):(<div className='w-full h-48 bg-gray-100 rounded-xl mb-3'/>) }<div className='flex items-start justify-between gap-2'><h3 className='font-semibold line-clamp-1'>{listing.title}</h3><span className='text-sm bg-gray-100 rounded-lg px-2 py-1 whitespace-nowrap'>{price}</span></div>{listing.isAnimal&&listing.breed&&<p className='text-xs text-gray-600 mt-1'>🐶 {listing.breed}</p>}<p className='text-sm text-gray-600 line-clamp-2 mt-1'>{listing.description}</p><div className='mt-2 flex items-center justify-between'><p className='text-xs text-gray-500'>📍 {listing.location||'—'}</p>{listing.category && <span className='badge'>{listing.category.name}</span>}</div></Link>); }